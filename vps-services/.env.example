# =============================================================================
# VPS Services Environment Configuration
# =============================================================================

# Admin API key for Playwright service (generate with: openssl rand -hex 32)
PLAYWRIGHT_ADMIN_API_KEY=your_admin_key_here

# Domain configuration
DOMAIN=api.productpraat.nl
EMAIL=admin@productpraat.nl

# =============================================================================
# Playwright API Settings
# =============================================================================

# Maximum concurrent browser tasks
MAX_CONCURRENT_TASKS=5

# Default rate limit (requests per minute per API key)
DEFAULT_RATE_LIMIT=60

# Session TTL in hours
SESSION_TTL_HOURS=24

# =============================================================================
# Optional: Bol.com Partner Credentials (for affiliate features)
# =============================================================================
# These credentials are used to log in to Partner Plaza
# for generating official affiliate deeplinks

BOL_PARTNER_EMAIL=
BOL_PARTNER_PASSWORD=

# =============================================================================
# Optional: Other Service Credentials
# =============================================================================
# Add credentials for other services your projects use

# TRADETRACKER_API_KEY=
# DAISYCON_API_KEY=
# AWIN_API_KEY=
